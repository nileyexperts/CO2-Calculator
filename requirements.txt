# --- App & UI ---
streamlit>=1.32,<2.0

# --- Données & calculs ---
pandas>=2.0
numpy>=1.24
requests>=2.31

# --- Géocodage & distances ---
opencage>=2.4
geopy>=2.4

# --- Cartographie interactive ---
pydeck>=0.8

# --- Cartographie statique & lecture de données (sans GDAL/Fiona) ---
geopandas>=0.14
shapely>=2.0
pyogrio>=0.7    # moteur d’E/S pour GeoPandas (évite GDAL/Fiona)
matplotlib>=3.7
pillow>=10.0    # logo + rendu image pour PDF

# --- (Optionnel) Si tu veux réactiver l’export PDF via PyMuPDF au lieu de Matplotlib :
# pymupdf>=1.23
